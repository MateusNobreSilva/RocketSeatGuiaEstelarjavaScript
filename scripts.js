const person = {
    name: 'Solarie',
    age: 28,
    weight: 1.80,
    idAdmin: true
}

console.log(person.name + ' tem ' + person.age +' anos');

console.log( `${person.name} tem ${person.age} anos` );